# Blog Work — Status & Handoff Ledger

**This is the living state of blog work.** It exists so anyone picking up — a new teammate, or a fresh AI session — knows exactly where things stand and what to do next, without needing any chat history. The other docs say *how* to make a blog; this says *where we are*.

> **Handoff protocol (everyone follows this):**
> - **Before you start:** read `docs/blog/README.md`, then this file.
> - **While you work:** keep the "In flight" table current.
> - **Before you leave:** update statuses, record blockers, and list the next actions. Push your branch. That's what lets the next person continue the series.

*Last updated: 2026-08-15.*

---

## In flight — branches, state, next action

| Blog / work | Branch | State | Open item / next action |
|---|---|---|---|
| From Sketch to Store — a real clothing production timeline | `blog/clothing-production-timeline` | Scaffolded; **build green (exit 0)**, route `ƒ /blogs/clothing-production-timeline` (17.3 kB); tsc clean; eslint clean; invariant 63==63; page returns **200** on the dev server | id **64** (63 is held by the unmerged `blog/private-label-vs-custom-clothing-manufacturing` branch). Manufacturing / Production &amp; Sourcing. Top-of-funnel informational pillar for the focus keyword **"clothing production timeline"**; JSON-LD `BlogPosting` + `HowTo` (10 steps) + `FAQPage` (5 Q) + `BreadcrumbList`. Body prose **1,638 words** (inside MASTER_PROMPT's 1,400–1,800), 12 H2 sections + FAQ, read time **11 min**. Branched off `main` at `087fa13`. **Content angle, as briefed:** mirrors and expands the homepage's *From Design to Shelf in 4 Months* section — the four H2 phase headings map 1:1 onto `src/components/FromDesignToShelf.tsx` (Design → Sampling &amp; Sourcing → Bulk Production → Quality Check &amp; Dispatch), which is the pillar-to-cluster link the brief asked for. ⚠️ **Editorial exception:** owner asked for plain, simple, human language, which softens MASTER_PROMPT's Bloomberg-trade register; specificity and sourcing kept intact. ⚠️ **Cannibalisation watch:** the older `lead-time-timeline-design-to-doorstep` (id 26, April 2026, 7 min, old template) covers adjacent ground. Kept live and **linked from this post** rather than rewritten — same "new post at a new slug" precedent the owner set for id 63. If rankings split, 301 id 26 to this one. ✅ **All 4 teaching graphics are inline SVG, not generated images**, so figures cannot drift: 01 the 23-week Gantt (10 stages, exactly 20px/week), 02 three lanes (16 / 23 / 29 weeks), 03 the backward planner (21 Sep 2026 → 1 Mar 2027 with the Lunar New Year blackout overlaid), 04 the five slip causes. **Every number reconciles:** phases 4+7+6+6 = 23 weeks; development 11 + making 7 + moving 5 = 23; 29−16 = the 13-week spread quoted in the body; 21 Sep 2026 + 161 days = 1 Mar 2027 exactly; the blackout band opens on the same day production ends (day 126 = 25 Jan 2027), which is the point the caption makes. Infographic 04 is **explicitly labelled as planning allowances, not measured averages** — it carries no sourced-statistic claim. **10 internal links** (every target confirmed to exist on this branch) + **6 external source links**. Facts verified live 2026-08-15: Freightos India→US ocean **30–40 days door-to-door**, air 8–10 days (page stamped "Updated August 2026"); CBP entry documents due **within 15 calendar days of arrival** (CBP ICP *Entry*); USITC HTS 6109.10.00 at **16.5%**; CBP de-minimis indefinite suspension (Fed. Reg. 2026-06-24); **Lunar New Year 2027 = 6 Feb**, factories at ~35% into late Feb, full recovery 6–20 Mar 2027 (SEKO Logistics); NRF holiday spend past **$1tn**. Diwali 8 Nov 2026 is stated as a calendar date only — shutdown length is framed qualitatively because no garment-industry-specific figure could be sourced. ⛔ **BLOCKER — 5 photos still missing** (all 5 slots return 404 today): `production-timeline-{hero,section1,sampling,macro,closing}.jpg` → prompts ready in **"Image prompts — id 64"** below. 🔁 **Post-merge follow-up:** 3 links currently point at the older live `private-label-vs-custom-manufacturing` (id 17) because the id-63 slug is not on `main` yet; **once id 63 merges, repoint them** to `/blogs/private-label-vs-custom-clothing-manufacturing` — the TOC-rail card, the inline link in "When four months is the wrong number", and the read-next card (also bump that card's "8 min read" to "7 min read"). **Local commit only — not pushed, no PR, per the owner's standing instruction.** |
| How to Start a Clothing Brand in 2026 — step-by-step guide | `blog/how-to-start-a-clothing-brand-2026` | Scaffolded; **build green (exit 0)**, route `ƒ /blogs/how-to-start-a-clothing-brand-2026`; tsc/eslint clean; invariant 57==57 | id **59** (57 reserved by `second-origin`, 58 by `de-minimis-hangover`). Business / Growth &amp; Business. Top-of-funnel SEO pillar for the focus keyword **"how to start a clothing brand"**; 8-step build order; JSON-LD `BlogPosting` + `HowTo` + `FAQPage` + `BreadcrumbList` for AEO. ⚠️ **Keyword cannibalisation, accepted by the owner:** the older post `how-to-start-clothing-brand-2026` (id 9, "Launching a Global Clothing Brand") holds the exact-match slug and stays live — two pages now target the same query. If rankings split, 301 the old slug to this one. ⚠️ **Editorial exception:** MASTER_PROMPT bans "How to" tutorial framing; overridden by the owner for SEO. ✅ **All 5 images in place and individually opened** (`start-clothing-brand-{hero,section1,teaching,macro,closing}.jpg`, 1024×1024, 5 distinct MD5s — no byte-dupes). Teaching graphic = "The First-Year Build Order"; its first cut had a broken axis (month 6 missing, 12 duplicated) and bars that overlapped/shared endpoints — **regenerated 2026-08-05 13:27 and re-verified**: axis now a clean 1–12 scale, all 8 rows labelled, bars strictly sequential, all bands match the body table (1–2 Niche · 2–3 Range plan · 3–4 Entity/trademark · 4–5 Tech pack · 5–7 Fabric &amp; sampling · 7–8 Landed cost &amp; labelling · 8–10 First production run · 10–12 Launch). Residual nit, not blocking: the Niche bar is drawn ~2 months wide, shifting later bars ~0.5 month right of the axis; every bar is individually labelled and the authoritative table sits directly below, so the sequence still reads correctly. Swap for an inline SVG if pixel-exact registration is ever wanted. **REVISION (2026-08-05, post-review):** owner review asked for more visuals, a stronger hook, real examples and an educate-first slant. Applied — new hook ("most brands don't fail on design, they fail on manufacturing", carried as judgement not statistic since it can't be sourced); 2 real-example blocks built from **verified** case-study copy (Badria Al Shihhi's documented 8-month launch; the sample-vs-bulk drift, citing Drover's wear tests + HY Official's SS24 Camp Shirt iterations) with links to `/case-studies/*`; private-label-vs-custom and tech-pack-omission comparison tables as **HTML, not images**, so they can't drift; 12-term plain-English glossary + `DefinedTermSet` JSON-LD; new FAQ "Why do most new clothing brands fail?". Read time 11→**15 min**, TOC + excerpt updated. ✅ **All 9 images now in place** (5 originals + techpack/fabric/sampling/costflow added 2026-08-05 15:44), 9 unique MD5s, each opened and each serving 200 byte-exact from the dev server. `costflow` verified digit-for-digit against the body table. ⛔ **ONE MERGE BLOCKER: `start-clothing-brand-techpack.jpg` has a garbled ninth callout reading "label and careolock"** — a nonsense word duplicating the legitimate "label and care-label placement"; also stray quote marks around 'flat sketch'. Regenerate with exactly 8 callouts. Minor nit, non-blocking: `sampling.jpg` draws three *different* garment types (shirt, dress, blazer) where the three rounds are of the same garment — text is correct, so optional. Numbers that must match the body: costflow = $12.00 + $0.85 + $1.98 + $0.30 + $0.45 = **$15.58**, caption "factory quote is 77% of the real number". Two further logged exceptions to MASTER_PROMPT: (a) case studies are named mid-body, breaking "KK only at the close" — deliberate, for the conversion path; (b) "MOQ" appears in the glossary as a neutral definition, never as a "low/zero MOQ" claim. Owner asked for "fabric swatches" — **declined as specified** (hard-banned flat-lay); delivered as cloth off the roll against raking light instead. Facts verified live (Statista US apparel $373bn 2026; BLS BED survival; USPTO $350 base + $100/$200 surcharges per FY2025 fee rule; CBP de-minimis indefinite suspension 2026-06-24; HTS 6109.10.00 at 16.5%; FTC Textile/Wool + Care Labeling Rule; NRF 15.8%/19.3% returns). **No direct-to-main.** |
| After the Cliff — landed-cost rebuild (Aug) | `blog/rebuild-landed-cost-august-2026` | Scaffolded; **build green (exit 0)**, route `ƒ /blogs/rebuild-landed-cost-august-2026`; invariant 55==55; PR flow | id **55**. Production & Sourcing. Sequel to the July-24 cliff post — the post-lapse *landed-cost rebuild* for Aug-clearing goods (duty follows entry date; 3 scenarios 16.5% / 26.5% / 29%; + MPF/HMF/brokerage; consolidate; pass-through clause). Facts verified on live web (CRS, White & Case, CBP fee table, Tariffs Tool, Greenwich). **Needs 4 images** in `public/blog/`: `rebuild-cost-august-hero.jpg`, `-section1.jpg`, `-teaching.jpg`, `-closing.jpg` (teaching = "The $14 tee's August landed cost, three ways" — columns must total **$16.43 / $17.83 / $18.18** to match body). Push branch → PR → await sign-off → merge. **No direct-to-main.** |
| Tennis-Core / Wimbledon (July #5) | `blog/tenniscore-wimbledon-us-brands-2026` | Scaffolded; **build green (exit 0)**; PR flow | id **52**. Culture & Brand. Facts verified on live web. **Needs 4 images** in `public/blog/`: `tenniscore-hero.jpg`, `-section1.jpg`, `-teaching.jpg`, `-closing.jpg` (teaching = "Anatomy of a Tenniscore Piece": 5 cues + 200–260 GSM + optic-white/cream/kelly-green/navy palette — must match body). Push branch → PR → await sign-off → merge. **No direct-to-main.** |
| Forced-labor 301 regime (10% vs 12.5%) | pushed direct to `main` (c4a573b, 2026-07-06) | ✅ Live | id 51. Merged fast-forward to main and pushed (no PR, per owner). 4 images in place, build green, invariant 51==51. Verify the provider deploy went green on the dashboard. |
| Tariff cliff (July 24 / Section 122) | merged to `main` (PR #47) | ✅ Live | Its byline on `main` still shows a persona name until the byline-fix (on the de-minimis branch) reaches `main`. |
| De-minimis ($800 rule) | `blog/de-minimis-end-us-brands-2026` | Pushed to origin; **not merged** | (1) Closing image is a **duplicate of the Section-1 macro** — replace `public/blog/de_minimis_closing.jpg` with the parcel-vs-pallet shot. (2) This branch also carries the **"Krazy Kreators Team" byline fix** for 6 older posts — merging it standardizes all bylines. Open a PR. |
| Osaka Wimbledon (walk-on) | `blog/naomi-osaka-wimbledon-kimono-us-brands-2026` | Pushed to origin; **not merged** | **Licensing** on the 3 real press photos (`.webp`) must be cleared before merge. Otherwise complete (6 images, build green). Open a PR. |
| Blog system docs (this folder) | `docs/blog-system-runbook` | Pushed to origin; **not merged** | Merge to `main` to make the pipeline + master prompt canonical for everyone. |

## Standing decisions (so nobody re-litigates them)
- **Byline = "Krazy Kreators Team"** + desk label. No persona names. (Set 2026-06-30.)
- **Images:** the assistant can't save chat-pasted images — files must land in `public/blog/`. Naming: `<slug>-hero`, `<slug>-section1`, `<slug>-teaching`, `<slug>-macro`, `<slug>-closing`.
- **One blog at a time** until the shared-`<BlogArticle>` refactor lands (avoids id collisions + the merge-drop hazard). See `PIPELINE.md`.

## Open blockers / decisions pending
- **Osaka press-photo licensing** — owner to confirm before merge.
- **De-minimis closing image** — swap the duplicate.
- **Deploy:** an earlier Vercel preview failed on the tariff PR while the local build passed → likely env/config/stale, not code. If a preview fails, read the provider dashboard log (the `gh`/`vercel` CLIs aren't authenticated in the working environment).

## Image prompts — id 64 (`blog/clothing-production-timeline`)

Save into `public/blog/` with these exact names. Five different registers, no two alike; pre-flighted against the id 59 and id 63 sets so nothing repeats. **No faces, no real logos or brand marks, small "KK" watermark bottom-right, one cohesive warm-neutral grade across all five.** The four teaching graphics are already built as inline SVG, so **no infographic needs generating** — these five are all photography.

1. **`production-timeline-hero.jpg`** — *slot: hero · register: abstract/conceptual · 16:9*
   > A long paper production calendar pinned across a studio wall in late afternoon, pencil-marked with weeks and arrows, a first flat sketch taped at the far left and a single folded finished garment resting on a shelf at the far right, one hand entering frame to mark a date. Shot on a Canon EOS-1V with a 35mm prime on Kodak Portra 400, low raking window light, natural grain, foreground slightly out of focus, deep depth layering. Not a render, not CGI, not over-smoothed, no faces, no logos.

2. **`production-timeline-section1.jpg`** — *slot: Section 1 (most-read) · register: documentary wide · 1:1*
   > A garment cutting room mid-run: pale cloth spread in dozens of flat layers down a long table, marker paper pinned over the top, a cutting head parked at the far end, rolls stacked against the wall behind. High windows throw hard raking daylight across the stack. Shot on a Nikon F100 with a 28mm prime on Kodak Portra 400, available light only, slight handheld tilt, visible grain, cavernous and lived-in. Not a render, not CGI, no faces, no brand marks.

3. **`production-timeline-sampling.jpg`** — *slot: Phase 2 · register: single garment / editorial · 1:1*
   > Three versions of the same unbranded shirt hanging in a row on a sample-room rail, each pinned and chalk-marked differently, a tape measure draped over the rail between them. One hard side light from a window rakes across the three, throwing their shadows onto a bare plaster wall. Shot on a Contax G2 with a 45mm prime on Kodak Portra 400, shallow depth of field falling off to the right, natural grain. Not a render, not CGI, no faces, no logos.

4. **`production-timeline-macro.jpg`** — *slot: Phase 4 · register: macro detail · 1:1*
   > Extreme macro into an open carton of finished stock: the crease of a folded polybagged garment, a plain printed size sticker catching the light, and the corrugated cardboard edge razor-sharp in the foreground, the rest of the stack dissolving into shallow focus behind. Shot on a 100mm macro at f/4 on Kodak Portra 160, soft north-window light, fine natural grain, no text legible beyond a size code. Not a render, not CGI, no logos.

5. **`production-timeline-closing.jpg`** — *slot: closing · register: documentary/cinematic wide · 16:9*
   > A quiet warehouse loading bay at dusk, roller door half open, one stack of sealed unmarked cartons on a pallet catching the last cold daylight through the gap, the truck bay beyond it empty and falling into shadow. Shot on a Leica M6 with a 35mm prime on CineStill 800T, available light, long shadows, muted teal-and-amber cast, natural grain, faint haze. Not a render, not CGI, no faces, no brand marks.

## Recommended next actions (in order)
0. **id 64 (clothing production timeline)** — generate the 5 photos above into `public/blog/`, then re-verify each opens individually and that no two share an MD5. Awaiting the owner's push command before the branch leaves the machine; then open its PR.
1. Decide licensing on the Osaka photos → open its PR.
2. Swap the de-minimis closing image → open its PR (this also lands the byline fix).
3. Merge `docs/blog-system-runbook` to `main` so the system is canonical.
4. Then merge the blog PRs one at a time, re-checking the `blogPosts.ts` count invariant after each (see `PIPELINE.md` § merge-drop).
5. When bandwidth allows: greenlight the shared-`<BlogArticle>` refactor (`PIPELINE.md`) to end the copy-paste.
